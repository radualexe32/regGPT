from model import *
from preload_model import *

print("This is where we will train our custom models?")
# TODO: Will need to train in google collab and save checkpoints so that we don't lose progress after 12 hours time limit expires.
